import React from "react";

const Menu = ({ menuOpenClose, setmenuOpenClose }) => {
    return (
        <div className="w-full h-screen fixed top-0 left-0 bg-white z-20"></div>
    );
};

export default Menu;
